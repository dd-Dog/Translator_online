# 论文级评测改进计划

**目标**: 实现论文级的翻译质量评估系统，支持多模型对比和完整的评估指标体系

**日期**: 2025-12-09

---

## 📋 需求分析

### 1. 添加ChrF评测体系
- **ChrF (Character n-gram F-score)**: 基于字符n-gram的F分数
- **优势**: 对形态变化丰富的语言（如中文）更友好
- **可行性**: ✅ 完全可行，有成熟的Python库支持

### 2. 单模型翻译支持
- **需求**: 支持单独使用单个模型进行翻译
  - Gemini
  - OpenAI (GPT-4)
  - Claude
  - Qwen
  - DeepSeek
- **目的**: 与多模型系统进行性能对比
- **可行性**: ✅ 完全可行，已有单模型接口

### 3. 统一评测体系
- **6个指标**: BLEU, COMET, BLEURT, BERTScore, MQM, ChrF
- **目的**: 生成对比表格，便于论文展示
- **可行性**: ✅ 完全可行，需要集成ChrF

### 4. FLORES-200数据集
- **数据集**: FLORES-200 (200种语言对)
- **用途**: 标准化的评估数据集
- **可行性**: ✅ 完全可行，Hugging Face提供

---

## 🎯 实现方案

### 阶段1: 添加ChrF评估器

#### 1.1 安装依赖
```bash
pip install sacrebleu
# 或
pip install nltk  # 如果使用NLTK实现
```

#### 1.2 创建ChrF评估器
**文件**: `src/evaluation/chrf_scorer.py`

**功能**:
- 计算ChrF分数（字符级F-score）
- 支持ChrF1, ChrF2, ChrF3（不同n-gram）
- 默认使用ChrF2（论文常用）

**接口设计**:
```python
class ChrFScorer:
    def __init__(self):
        """初始化ChrF评估器"""
    
    def score(self, source: str, translation: str, reference: str) -> float:
        """计算ChrF分数"""
        # 返回ChrF2分数（0-1范围）
```

#### 1.3 集成到CombinedQualityScorer
**修改**: `src/evaluation/combined_scorer.py`

**添加**:
- `use_chrf: bool = True` 参数
- `chrf_scorer: ChrFScorer` 实例
- `chrf` 字段到 `ComprehensiveScore`
- ChrF计算逻辑

---

### 阶段2: 单模型翻译支持

#### 2.1 创建单模型翻译器
**文件**: `src/agents/single_model_translator.py`

**功能**:
- 封装单个模型的翻译接口
- 支持所有模型类型（Gemini, OpenAI, Claude, Qwen, DeepSeek）
- 提供统一的翻译接口

**接口设计**:
```python
class SingleModelTranslator:
    def __init__(self, model_type: str, model_config: dict):
        """初始化单模型翻译器
        
        Args:
            model_type: 'gemini', 'openai', 'claude', 'qwen', 'deepseek'
            model_config: 模型配置（从config/models.yaml读取）
        """
    
    async def translate(
        self, 
        text: str, 
        source_lang: str = "auto",
        target_lang: str = "zh"
    ) -> str:
        """执行翻译，返回翻译结果"""
```

#### 2.2 创建批量翻译脚本
**文件**: `evaluate_single_models.py`

**功能**:
- 对每个单模型执行翻译
- 使用相同的测试数据集
- 保存翻译结果

**流程**:
1. 加载FLORES-200数据集
2. 对每个模型类型：
   - 初始化模型
   - 翻译所有样本
   - 保存结果到JSON
3. 生成翻译结果文件

---

### 阶段3: 统一评估系统

#### 3.1 扩展CombinedQualityScorer
**修改**: `src/evaluation/combined_scorer.py`

**添加6个指标**:
- ✅ BLEU (已有)
- ✅ COMET (已有，可选)
- ✅ BLEURT (已有，可选)
- ✅ BERTScore (已有)
- ⚠️ MQM (已有，但需要适配单模型)
- ⬜ ChrF (新增)

**问题**: MQM需要Checker Agent，单模型没有
**解决方案**: 
- 方案A: 单模型使用简化的MQM（基于规则）
- 方案B: 单模型使用GPT-4评估MQM（但会增加成本）
- 方案C: 单模型MQM设为0或N/A，在表格中标注

**推荐**: 方案C（在论文中说明单模型没有多阶段质量检查）

#### 3.2 创建统一评估脚本
**文件**: `evaluate_paper_grade.py`

**功能**:
- 评估多模型系统
- 评估所有单模型
- 生成对比表格

**输出格式**:
- JSON: 详细数据
- Markdown: 可读表格
- CSV: Excel导入
- LaTeX: 论文表格

---

### 阶段4: FLORES-200数据集集成

#### 4.1 数据集准备
**文件**: `scripts/download_flores.py`

**功能**:
- 从Hugging Face下载FLORES-200
- 选择需要的语言对（如en→zh, ja→zh, fr→zh）
- 转换为项目格式

**数据格式**:
```json
{
    "id": 1,
    "source_lang": "en",
    "target_lang": "zh",
    "source": "English text",
    "reference": "中文参考翻译",
    "domain": "general"
}
```

#### 4.2 数据集配置
**文件**: `config/datasets.yaml`

**配置**:
```yaml
flores200:
  name: "FLORES-200"
  source: "huggingface"
  path: "data/flores200"
  languages:
    - en-zh
    - ja-zh
    - fr-zh
  sample_size: 100  # 每个语言对采样数量
```

---

## 📊 评估流程设计

### 完整评估流程

```
1. 数据准备
   ├─ 下载FLORES-200数据集
   ├─ 选择语言对（en→zh, ja→zh, fr→zh等）
   └─ 转换为项目格式

2. 多模型系统评估
   ├─ 使用完整Pipeline翻译
   ├─ 使用6个指标评估
   └─ 保存结果

3. 单模型评估（循环每个模型）
   ├─ Gemini翻译
   ├─ OpenAI翻译
   ├─ Claude翻译
   ├─ Qwen翻译
   └─ DeepSeek翻译
   └─ 每个模型使用6个指标评估

4. 结果汇总
   ├─ 生成对比表格
   ├─ 统计分析
   └─ 生成报告
```

---

## 📈 输出格式设计

### 对比表格格式

#### Markdown表格
```markdown
| 模型 | BLEU | COMET | BLEURT | BERTScore | MQM | ChrF | 平均 |
|------|------|-------|--------|-----------|-----|------|------|
| 多模型系统 | 0.75 | 0.82 | 0.78 | 0.97 | 0.90 | 0.73 | 0.83 |
| Gemini | 0.72 | 0.80 | 0.75 | 0.95 | N/A | 0.70 | 0.74 |
| GPT-4 | 0.74 | 0.81 | 0.77 | 0.96 | N/A | 0.72 | 0.76 |
| Claude | 0.73 | 0.79 | 0.76 | 0.95 | N/A | 0.71 | 0.75 |
| Qwen | 0.71 | 0.78 | 0.74 | 0.94 | N/A | 0.69 | 0.73 |
| DeepSeek | 0.70 | 0.77 | 0.73 | 0.93 | N/A | 0.68 | 0.72 |
```

#### LaTeX表格（论文用）
```latex
\begin{table}[h]
\centering
\caption{翻译质量评估对比}
\begin{tabular}{lcccccc}
\toprule
模型 & BLEU & COMET & BLEURT & BERTScore & MQM & ChrF \\
\midrule
多模型系统 & 0.75 & 0.82 & 0.78 & 0.97 & 0.90 & 0.73 \\
Gemini & 0.72 & 0.80 & 0.75 & 0.95 & N/A & 0.70 \\
GPT-4 & 0.74 & 0.81 & 0.77 & 0.96 & N/A & 0.72 \\
\bottomrule
\end{tabular}
\end{table}
```

---

## 🔧 技术实现细节

### 1. ChrF实现

**方案A: 使用sacrebleu（推荐）**
```python
from sacrebleu.metrics import CHRF

chrf = CHRF()
score = chrf.corpus_score([translation], [[reference]]).score / 100
```

**方案B: 使用NLTK**
```python
from nltk.translate.chrf_score import sentence_chrf

score = sentence_chrf(reference, translation)
```

**推荐**: 方案A（sacrebleu是WMT官方推荐）

### 2. 单模型翻译实现

**架构**:
```
SingleModelTranslator
├─ 模型工厂模式
│  ├─ create_gemini_translator()
│  ├─ create_openai_translator()
│  ├─ create_claude_translator()
│  ├─ create_qwen_translator()
│  └─ create_deepseek_translator()
└─ 统一翻译接口
```

### 3. MQM处理方案

**多模型系统**: 使用Checker Agent的MQM评分

**单模型系统**: 
- 选项1: 使用GPT-4评估（增加成本）
- 选项2: 设为N/A（推荐，在论文中说明）
- 选项3: 使用简化规则（不推荐，不准确）

**推荐**: 选项2，在论文中说明：
> "单模型系统没有多阶段质量检查流程，因此MQM指标不适用（标记为N/A）。多模型系统通过Checker Agent进行一致性检查和MQM评分。"

### 4. 数据集处理

**FLORES-200特点**:
- 200种语言对
- 每个语言对约1000条样本
- 涵盖多个领域

**处理策略**:
- 选择常用语言对（en→zh, ja→zh, fr→zh等）
- 每个语言对采样100-200条（根据计算资源）
- 保持领域平衡

---

## 📁 文件结构

### 新增文件

```
src/
├── evaluation/
│   └── chrf_scorer.py          # ChrF评估器（新增）
├── agents/
│   └── single_model_translator.py  # 单模型翻译器（新增）
scripts/
├── download_flores.py           # 下载FLORES-200（新增）
config/
└── datasets.yaml                # 数据集配置（新增）

evaluate_paper_grade.py          # 论文级评估主脚本（新增）
evaluate_single_models.py        # 单模型评估脚本（新增）
```

### 修改文件

```
src/evaluation/
└── combined_scorer.py           # 添加ChrF支持

config/
└── evaluation.yaml              # 添加ChrF配置
```

---

## ⏱️ 实施时间表

### 第1周: ChrF集成
- [ ] 安装sacrebleu
- [ ] 实现ChrFScorer
- [ ] 集成到CombinedQualityScorer
- [ ] 测试验证

### 第2周: 单模型翻译
- [ ] 实现SingleModelTranslator
- [ ] 创建单模型评估脚本
- [ ] 测试所有模型类型

### 第3周: FLORES-200集成
- [ ] 下载FLORES-200数据集
- [ ] 数据格式转换
- [ ] 数据集配置

### 第4周: 统一评估系统
- [ ] 实现evaluate_paper_grade.py
- [ ] 生成对比表格
- [ ] 生成LaTeX表格
- [ ] 完整测试

---

## 🎯 评估指标说明

### 1. BLEU
- **类型**: n-gram匹配
- **范围**: 0-1
- **特点**: 传统基准，表面匹配

### 2. COMET
- **类型**: 神经网络质量估计
- **范围**: 0-1
- **特点**: WMT官方标准，语义理解

### 3. BLEURT
- **类型**: 神经网络
- **范围**: 0-1
- **特点**: Google开发，需要GPU

### 4. BERTScore
- **类型**: 语义相似度
- **范围**: 0-1
- **特点**: 基于BERT，不依赖表面匹配

### 5. MQM
- **类型**: 多维度质量指标
- **范围**: 0-1
- **特点**: 人工评估标准（充分性、流畅性、术语）
- **注意**: 单模型系统不适用

### 6. ChrF
- **类型**: 字符n-gram F-score
- **范围**: 0-1
- **特点**: 对形态变化丰富的语言友好

---

## 📊 论文表格设计

### 主表格: 整体性能对比

| 模型 | BLEU | COMET | BLEURT | BERTScore | MQM | ChrF | 平均* |
|------|------|-------|--------|-----------|-----|------|-------|
| 多模型系统 | X.XX | X.XX | X.XX | X.XX | X.XX | X.XX | X.XX |
| Gemini | X.XX | X.XX | X.XX | X.XX | N/A | X.XX | X.XX |
| GPT-4 | X.XX | X.XX | X.XX | X.XX | N/A | X.XX | X.XX |
| Claude | X.XX | X.XX | X.XX | X.XX | N/A | X.XX | X.XX |
| Qwen | X.XX | X.XX | X.XX | X.XX | N/A | X.XX | X.XX |
| DeepSeek | X.XX | X.XX | X.XX | X.XX | N/A | X.XX | X.XX |

*注: 平均分不包括MQM（单模型系统不适用）

### 子表格: 按语言对统计

#### 英语→中文
| 模型 | BLEU | COMET | BERTScore | ChrF |
|------|------|-------|-----------|------|
| 多模型系统 | X.XX | X.XX | X.XX | X.XX |
| ... | ... | ... | ... | ... |

#### 日语→中文
（同上格式）

---

## ⚠️ 注意事项

### 1. 计算资源
- **COMET**: 需要2-3分钟/10句（CPU）
- **BLEURT**: 需要GPU，否则很慢
- **建议**: 开发阶段只用BERTScore+ChrF，论文阶段再启用COMET

### 2. API成本
- **单模型评估**: 需要调用5个模型的API
- **成本估算**: 100条样本 × 5模型 = 500次API调用
- **建议**: 先用小样本测试，确认流程后再全量评估

### 3. MQM处理
- **多模型**: 使用Checker Agent的MQM
- **单模型**: 标记为N/A（在论文中说明原因）

### 4. 数据集选择
- **FLORES-200**: 标准数据集，但可能较大
- **建议**: 先选择3-5个常用语言对，每个100-200条样本

### 5. 评估一致性
- **确保**: 所有模型使用相同的测试集
- **确保**: 所有模型使用相同的评估指标
- **确保**: 评估参数一致

---

## 🚀 快速开始（实施后）

### 1. 安装依赖
```bash
pip install sacrebleu
pip install unbabel-comet  # 如果需要COMET
pip install datasets  # 用于下载FLORES-200
```

### 2. 下载数据集
```bash
python scripts/download_flores.py
```

### 3. 运行评估
```bash
# 评估多模型系统
python evaluate_paper_grade.py --mode multi

# 评估所有单模型
python evaluate_paper_grade.py --mode single --model all

# 完整评估（多模型+所有单模型）
python evaluate_paper_grade.py --mode all
```

### 4. 生成表格
```bash
# 生成Markdown表格
python evaluate_paper_grade.py --output markdown

# 生成LaTeX表格
python evaluate_paper_grade.py --output latex

# 生成CSV表格
python evaluate_paper_grade.py --output csv
```

---

## 📝 论文写作建议

### 1. 实验设置部分
- 说明使用的数据集（FLORES-200）
- 说明评估指标（6个指标）
- 说明对比模型（5个单模型 + 多模型系统）

### 2. 结果分析部分
- 展示对比表格
- 分析多模型系统的优势
- 讨论各指标的差异

### 3. 讨论部分
- 说明MQM在单模型系统中不适用
- 分析不同指标反映的不同方面
- 讨论计算成本和实际应用的平衡

---

## ✅ 可行性总结

| 需求 | 可行性 | 难度 | 时间 |
|------|--------|------|------|
| 添加ChrF | ✅ 完全可行 | 低 | 1-2天 |
| 单模型翻译 | ✅ 完全可行 | 中 | 3-5天 |
| 统一评估体系 | ✅ 完全可行 | 中 | 3-5天 |
| FLORES-200 | ✅ 完全可行 | 低 | 1-2天 |
| **总计** | **✅ 完全可行** | **中** | **2-3周** |

---

## 🎯 下一步行动

1. **确认方案**:  review本计划，确认技术方案
2. **准备环境**: 安装必要依赖（sacrebleu等）
3. **分阶段实施**: 按照时间表逐步实现
4. **测试验证**: 每个阶段完成后进行测试
5. **生成报告**: 最终生成论文所需的对比表格

---

**创建时间**: 2025-12-09  
**状态**: 待实施  
**优先级**: 高（论文需求）

