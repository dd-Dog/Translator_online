# FastAPI核心
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0

# 数据验证
pydantic>=2.5.0
pydantic-settings>=2.1.0

# 安全
slowapi>=0.1.9  # 速率限制
python-multipart>=0.0.6  # 文件上传支持

# 工具
python-dotenv>=1.0.0
pyyaml>=6.0
aiofiles>=23.2.0  # 异步文件操作
requests>=2.31.0  # HTTP请求（用于调用评估服务）

# 日志
loguru>=0.7.0

# 数据库（可选，用于历史记录）
# sqlalchemy>=2.0.0
# aiosqlite>=0.19.0  # SQLite异步支持

