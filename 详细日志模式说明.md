# 详细日志模式完整说明

## 功能概述

通过详细日志模式，可以完整查看6个阶段的交互过程，包括：
- ✅ 每个阶段的输入提示词
- ✅ 模型的原始输出（未处理）
- ✅ 解析后的结果
- ✅ 评分变化过程
- ✅ 决策逻辑

## 使用方式

```bash
python test_verbose_pipeline.py
```

## 输出内容详解

### 完整的6阶段交互过程

#### 阶段1: Task Planner
```
💬 输入提示词: [Planner的完整提示词]
📤 模型原始输出: [GPT-4返回的任务计划]
✅ 解析结果: 检测语言、任务拆分详情
```

#### 阶段2: Translator-A (Gemini)
```
💬 输入提示词: [翻译提示词]
📤 模型原始输出: [Gemini返回的完整响应]
✅ 解析结果: 提取翻译文本和自检报告
```

#### 阶段3: Translator-B (Qwen)
```
💬 输入提示词: [翻译提示词]
📤 模型原始输出: [Qwen返回的完整响应]
✅ 解析结果: 提取翻译文本和自检报告
📊 两个翻译对比: 一致或存在差异
```

#### 阶段4: Checker (GPT-4) ⭐ 关键阶段
```
💬 输入提示词: [包含原文和两个翻译的评估提示词]
📤 模型原始输出 (JSON): [GPT-4返回的JSON评分]
✅ 解析结果:
  - 一致性检查
  - 冲突/遗漏/误解
  - MQM质量评分（两个翻译的平均分）
    * 翻译A评分: adequacy, fluency, terminology, overall
    * 翻译B评分: adequacy, fluency, terminology, overall
    * 平均评分: (A+B)/2
  - 选择最佳翻译
```

**重要说明**:
- MQM评分是**两个翻译的平均分**
- GPT-4会分别给翻译A和翻译B打分
- 最终使用平均值作为该段落的评分

#### 阶段5: Stylist (Qwen)
```
💬 输入提示词: [风格化提示词，包含术语表]
📤 模型原始输出: [Qwen返回的风格化结果]
✅ 解析结果:
  - 风格化前后对比
  - 术语变更记录
  - 风格变更记录
```

#### 阶段6: Aggregator (GPT-4)
```
💬 输入提示词: [整合提示词，包含所有前置结果]
📤 模型原始输出: [GPT-4返回的最终翻译]
✅ 解析结果:
  - 最终翻译
  - 质量评分变化（Checker评分 → 最终评分）
  - 提升幅度（固定+0.05）
  - 修改记录
```

## 关键发现

### 发现1: Checker评分逻辑

**问题**: MQM评分是针对翻译A还是翻译B？

**答案**: **两者的平均分**

**代码逻辑**:
```python
# src/agents/checker.py: 第162-183行
translator_scores = []
for translator, scores in scores_data.items():
    translator_scores.append({
        'adequacy': scores.get('adequacy', 0.8),
        'fluency': scores.get('fluency', 0.8),
        'terminology': scores.get('terminology', 0.8),
        'overall': scores.get('overall', 0.8)
    })

# 取平均分
avg_overall = sum(s['overall'] for s in translator_scores) / len(translator_scores)
```

**示例**:
- 翻译A评分: 0.92
- 翻译B评分: 0.89
- **最终Checker评分**: (0.92 + 0.89) / 2 = **0.905**

### 发现2: 评分提升逻辑

**Aggregator固定提升+0.05**:
```python
# src/agents/aggregator.py: 第210-214行
final_quality = QualityScore(
    adequacy=min(1.0, original_score.adequacy + 0.05),
    fluency=min(1.0, original_score.fluency + 0.05),
    terminology=min(1.0, original_score.terminology + 0.05),
    overall=min(1.0, original_score.overall + 0.05)
)
```

**示例**:
- Checker评分: 0.905
- 最终评分: 0.905 + 0.05 = **0.955**

### 发现3: 翻译A返回空的问题

从详细日志可以看到：
```
翻译A (Gemini): (空)
翻译B (Qwen): [机器学习是...]
```

**原因**: Planner误判语言，把英文当成中文
**影响**: Translator-A收到错误的输入
**解决**: 需要修复Planner的语言检测

## 通过详细日志发现的问题

### 问题1: 固定0.85评分（已解决）

**日志显示**:
```
[DEBUG] Checker (GPT-4) 原始响应: (空)
[DEBUG] 解析后的评分: 0.8（默认值）
```

**原因**: OpenRouter余额不足  
**解决**: 充值后GPT-4正常返回JSON

### 问题2: 翻译A返回空（新发现）

**日志显示**:
```
阶段2: Translator-A
输入提示词: 请将以下English文本翻译成中文
原文: 机器学习是...（已经是中文！）
模型原始输出: (空)
```

**原因**: Planner把英文误判为中文  
**影响**: 后续阶段输入错误  
**需要修复**: Planner的语言检测逻辑

### 问题3: 占位符问题（已发现）

**日志显示**:
```
翻译B输出: [机器学习是...]（带方括号）
```

**原因**: 模型返回带方括号的占位符格式  
**需要修复**: 解析时去除方括号

## 价值体现

### 透明化

- ✅ 看到每个模型的实际输入和输出
- ✅ 理解评分计算过程
- ✅ 发现隐藏的问题

### 可调试

- ✅ 立即定位Planner语言检测错误
- ✅ 发现翻译A返回空的原因
- ✅ 看到占位符问题

### 可优化

- ✅ 基于实际输出调整提示词
- ✅ 优化评分逻辑
- ✅ 改进错误处理

## 下一步改进

基于详细日志发现的问题：

1. **修复Planner语言检测**（紧急）
2. **修复占位符解析**（重要）
3. **优化评分逻辑**（可选）
   - 考虑是否需要平均分
   - 或者选择最佳翻译的评分

---

**创建时间**: 2025-12-04  
**状态**: 详细日志模式已实现  
**下一步**: 修复发现的问题

