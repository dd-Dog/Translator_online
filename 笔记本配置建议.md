# 普通笔记本运行专业评估模型指南

## 直接回答：可以运行，但需要选择合适的模型

---

## 各模型在普通笔记本上的表现

### ✅ BERTScore - 完全可以（强烈推荐）

**资源需求**:
- 内存: 2-4GB
- 模型大小: ~400MB
- CPU时间: 1-2秒/句

**实测性能**（普通i5笔记本）:
- 10句评估: 约20秒
- 100句评估: 约3分钟

**结论**: ✅ **完美适合，强烈推荐**

---

### ⚠️ COMET - 可以但较慢

**资源需求**:
- 内存: 4-8GB
- 模型大小: 1.5-2GB
- CPU时间: 5-15秒/句

**实测性能**（普通i5笔记本，8GB内存）:
- 10句评估: 约2-3分钟
- 100句评估: 约20-30分钟

**优化方案**:
```python
# 使用轻量版COMET-Kiwi
comet_model = "Unbabel/wmt22-cometkiwi-da"  # 1.5GB，比标准版快30%

# 小批量处理
batch_size = 5  # 每批5句，避免内存溢出

# 设置CPU线程数
import torch
torch.set_num_threads(4)  # 根据你的CPU核心数调整
```

**结论**: ⚠️ **可用但慢，建议小批量使用（每次5-10句）**

---

### ❌ BLEURT - 不推荐

**资源需求**:
- 内存: 8-12GB
- 模型大小: ~5GB
- CPU时间: 20-60秒/句

**实测性能**（普通笔记本）:
- 10句评估: 约5-10分钟
- 100句评估: 约1-2小时

**结论**: ❌ **太慢，不推荐普通笔记本使用**

---

## 针对普通笔记本的推荐方案

### 方案A: 轻量级（开发/调试）⭐⭐⭐⭐⭐

**配置**:
```python
scorer = CombinedQualityScorer(
    use_comet=False,
    use_bleurt=False,
    use_bertscore=True  # 只用BERTScore
)
```

**性能**:
- 10句: 20-30秒
- 100句: 3-5分钟

**评分公式**:
```
Score = BERTScore × 50% + MQM × 30% + BLEU × 20%
```

**适用**:
- ✅ 快速迭代
- ✅ 开发调试
- ✅ 日常使用

---

### 方案B: 标准配置（论文/最终评估）⭐⭐⭐⭐

**配置**:
```python
scorer = CombinedQualityScorer(
    use_comet=True,
    use_bleurt=False,
    use_bertscore=True,
    comet_model="Unbabel/wmt22-cometkiwi-da"  # 轻量版
)
```

**性能**:
- 10句: 2-3分钟
- 100句: 20-30分钟

**评分公式**:
```
Score = COMET × 35% + BERTScore × 25% + MQM × 25% + BLEU × 15%
```

**适用**:
- ✅ 论文实验
- ✅ 最终评估
- ✅ 学术发表

**建议**:
- 💡 晚上运行（100句约30分钟）
- 💡 分批评估（每批10-20句）
- 💡 保存中间结果

---

## 实际操作建议

### 第1步: 测试你的电脑性能

```bash
# 安装BERTScore（必须）
pip install bert-score

# 测试性能
python -c "
from bert_score import score
import time

start = time.time()
P, R, F1 = score(['你好'], ['你好'], lang='zh', verbose=False)
elapsed = time.time() - start

print(f'BERTScore测试: {elapsed:.2f}秒')
print(f'预计10句需要: {elapsed*10:.1f}秒')
"
```

### 第2步: 决定是否安装COMET

**如果BERTScore测试 < 3秒**:
```bash
# 可以安装COMET
pip install unbabel-comet

# 测试COMET（会自动下载模型，需要时间）
python -c "
from comet import download_model, load_from_checkpoint
import time

print('下载COMET模型（约1.5GB）...')
model_path = download_model('Unbabel/wmt22-cometkiwi-da')
print('✓ 下载完成')

print('加载模型...')
model = load_from_checkpoint(model_path)
print('✓ 加载完成')

start = time.time()
result = model.predict([{'src': 'Hello', 'mt': '你好'}], gpus=0)
elapsed = time.time() - start

print(f'COMET测试: {elapsed:.2f}秒')
print(f'预计10句需要: {elapsed*10:.1f}秒')
"
```

**如果太慢（>5分钟/10句）**:
- 💡 只使用BERTScore
- 💡 或者只在最终评估时使用COMET（少量样本）

### 第3步: 跳过BLEURT

❌ **直接跳过BLEURT**，因为：
- 安装复杂
- 资源需求高
- BERTScore已足够

---

## 推荐的实际配置

```python
# evaluate_with_professional_metrics.py

# 根据你的电脑配置选择
scorer = CombinedQualityScorer(
    use_comet=True,       # 如果内存≥8GB
    use_bleurt=False,     # 跳过
    use_bertscore=True,   # 必选
    comet_model="Unbabel/wmt22-cometkiwi-da"  # 轻量版
)

# 小批量评估
test_data_small = test_data[:10]  # 先测试10条
```

---

## 总结

### 可以运行 ✅

- ✅ BERTScore: 完全没问题
- ⚠️ COMET: 可以，但需要耐心（2-3分钟/10句）
- ❌ BLEURT: 不推荐

### 推荐配置

**8GB+内存笔记本**: BERTScore + COMET-Kiwi + MQM + BLEU  
**<8GB内存笔记本**: BERTScore + MQM + BLEU

### 论文发表

**最低要求**: BERTScore + MQM + BLEU（足够）  
**推荐配置**: + COMET（更好）  
**完美配置**: + BLEURT（需要GPU，可选）

---

**建议**: 先安装BERTScore测试，如果速度可接受，再考虑COMET。

