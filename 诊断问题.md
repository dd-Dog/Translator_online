# 系统评分问题诊断报告

## 问题现象

所有样本的系统评分都固定为 **0.85**，无区分度。

## 问题定位

通过添加调试代码，发现 **Checker (GPT-4) 返回空响应**。

### 调试输出

```
[DEBUG] Checker (GPT-4) 原始响应:
======================================================================

======================================================================
```

响应为空！

## 根本原因

### 原因1: OpenRouter余额不足 ⚠️⚠️⚠️

测试OpenAI模型时返回错误：

```
Error code: 402 - This request requires more credits, or fewer max_tokens. 
You requested up to 2000 tokens, but can only afford 1315.
```

**分析**:
- OpenRouter账户只剩1315个token的额度
- 但配置的max_tokens=2000
- 导致请求被拒绝，返回空响应

### 原因2: max_tokens设置过高

当前配置：
- OpenAI: max_tokens=2000
- Gemini: max_tokens未设置（使用默认）
- Qwen: max_tokens=2000

**问题**:
- 每次请求最多消耗2000 tokens
- 6个阶段 × 2000 tokens = 12000 tokens/次翻译
- 账户余额1315 tokens无法支撑

## 解决方案

### 方案1: 降低max_tokens（立即）

**修改配置**:

```yaml
# config/models.yaml
openai:
  params:
    max_tokens: 500  # 从2000降低到500
```

**预期**:
- 每次请求最多500 tokens
- 6阶段 × 500 = 3000 tokens/次
- 可以完成约0-1次完整翻译（余额1315）

### 方案2: 充值OpenRouter账户（推荐）

访问: https://openrouter.ai/settings/credits

**充值建议**:
- 最少: $5（约500K tokens）
- 推荐: $10-20（约1-2M tokens）
- 用于: 实验评估和改进

### 方案3: 使用免费模型（临时）

替换为免费或低成本模型：

```yaml
openai:
  model: "meta-llama/llama-3.2-3b-instruct:free"  # 免费模型
  # 或
  model: "google/gemini-2.5-flash"  # 成本较低
```

## 当前状态

- ✅ 系统代码正确
- ✅ JSON模式已启用
- ❌ OpenRouter余额不足（1315 tokens）
- ❌ max_tokens设置过高（2000）

## 下一步行动

### 立即执行

1. **降低max_tokens**: 500（已修改配置）
2. **重新测试**: 运行 `python test_checker_debug.py`
3. **验证响应**: 查看GPT-4是否返回有效JSON

### 短期执行

4. **充值OpenRouter**: 至少$5
5. **完整评估**: 运行 `python evaluate_baseline.py`
6. **验证改进**: 查看评分是否有区分度

## 预期结果

充值并降低max_tokens后：
- ✅ GPT-4返回有效JSON
- ✅ 评分有区分度（0.7-0.95）
- ✅ BLEU与系统评分正相关

---

**问题定位**: ✅ 完成  
**根本原因**: OpenRouter余额不足 + max_tokens过高  
**解决方案**: 降低max_tokens + 充值

