# 查看日志文件说明

## ✅ 文件编码确认

经过检查，日志文件 **已经是UTF-8编码**，可以正常显示中文。

**文件**: `V2_evaluation_verbose_log_20251208_144752.txt`  
**编码**: UTF-8  
**状态**: ✓ 正确

---

## 📖 如何查看（如果显示乱码）

### 方法1: VS Code（推荐）

1. 用VS Code打开文件
2. 查看右下角状态栏，应该显示 **"UTF-8"**
3. 如果显示乱码：
   - 点击右下角的编码（如"GBK"或"UTF-8"）
   - 选择 **"通过编码重新打开"**
   - 选择 **"UTF-8"**

### 方法2: Notepad++

1. 用Notepad++打开文件
2. 菜单：**编码** → **转为UTF-8编码**
3. 如果还是乱码，尝试：**编码** → **使用UTF-8编码**

### 方法3: Windows记事本

1. 用记事本打开文件
2. 菜单：**文件** → **另存为**
3. 在"编码"下拉框中选择 **"UTF-8"**
4. 保存后重新打开

### 方法4: Python查看（确保UTF-8）

```python
with open('V2_evaluation_verbose_log_20251208_144752.txt', 'r', encoding='utf-8') as f:
    content = f.read()
    print(content)
```

---

## 🔍 文件内容验证

我已经用Python检查过文件：

```
✓ 文件可以UTF-8编码读取
文件大小: 7016 字符
行数: 283
包含中文: True
```

**包含的内容**:
- ✅ 中文标题和说明
- ✅ 评估过程的详细日志
- ✅ 各阶段的提示词和输出
- ✅ 评分计算过程

---

## 💡 如果还是乱码

### 可能的原因：

1. **编辑器默认编码不是UTF-8**
   - 解决：手动选择UTF-8编码

2. **文件在传输过程中被转换**
   - 解决：重新生成文件（使用 `evaluate_v2_verbose_fixed.py`）

3. **系统区域设置问题**
   - 解决：确保系统支持UTF-8

### 快速测试：

运行以下Python代码验证文件：

```python
# check_log.py
with open('V2_evaluation_verbose_log_20251208_144752.txt', 'r', encoding='utf-8') as f:
    lines = f.readlines()
    print(f"总行数: {len(lines)}")
    print(f"\n前5行:")
    for i, line in enumerate(lines[:5], 1):
        print(f"{i}: {line.rstrip()}")
    
    # 查找包含中文的行
    chinese_lines = [line for line in lines if any('\u4e00' <= c <= '\u9fff' for c in line)]
    print(f"\n包含中文的行数: {len(chinese_lines)}")
    print(f"示例（第1行）: {chinese_lines[0][:80] if chinese_lines else '无'}")
```

如果这个脚本能正常显示中文，说明文件编码是正确的，问题在于你的编辑器设置。

---

## 📝 文件内容示例

文件应该包含类似这样的内容：

```
================================================================================
V2专业评估系统 - 详细日志模式（UTF-8编码）
================================================================================

【1/3】初始化专业评估模型（开发模式）
--------------------------------------------------------------------------------
正在初始化BERTScore...
✓ BERTScore已就绪

【2/3】加载测试数据
--------------------------------------------------------------------------------
✓ 加载 10 条测试样本

【3/3】创建翻译系统
--------------------------------------------------------------------------------
✓ 翻译系统创建成功（详细日志模式）

================================================================================
开始评估（详细日志模式）
================================================================================

[1/10] 评估样本 1
================================================================================

样本信息:
  领域: technology
  原文: Machine learning is a subset of artificial intelligence.
  参考翻译: 机器学习是人工智能的一个子集。
...
```

---

## ✅ 总结

1. **文件编码**: UTF-8 ✓
2. **中文内容**: 可以正常读取 ✓
3. **查看方法**: 用VS Code或Notepad++，确保选择UTF-8编码

如果按照上述方法还是显示乱码，请告诉我具体是哪个编辑器，我可以提供更具体的解决方案。

