# V2版本改进计划

**分支**: V2  
**基于**: V1（基线版本）  
**创建时间**: 2025-12-04  
**目标**: 提高评分系统的客观性和准确性

---

## 版本说明

### V1 (基线版本) ✅

**已完成的功能**:
- ✅ 6阶段多模型协作工作流
- ✅ 基本的MQM评分系统
- ✅ JSON模式强制输出
- ✅ 详细日志模式
- ✅ 9种风格配置
- ✅ 数据集评估框架

**V1的问题**:
- ⚠️ 单一评分者（仅GPT-4）
- ⚠️ 评分偏高（平均0.988）
- ⚠️ BLEU相关性低（0.10）
- ⚠️ 固定提升值（+0.05）
- ⚠️ 缺少客观指标

**V1评估结果**:
- 成功率: 100%
- 平均BLEU: 0.8240
- 平均系统评分: 0.9880
- 评分区分度: 0.94-1.00

---

## V2改进目标

### 核心目标

1. **提高评分客观性** 🎯
   - 多模型评分投票
   - 添加客观指标
   - 降低评分偏差

2. **提高评分准确性** 🎯
   - 评分与BLEU相关性 >0.5
   - 评分范围更广（0.7-0.95）
   - 更好的区分度

3. **完善评分系统** 🎯
   - 细粒度错误分类
   - 动态评分提升
   - 可校准的评分

---

## 改进计划

### 阶段1: 多模型评分投票（优先级1）

**目标**: 使用多个模型评分，减少单一模型偏见

**实现方案**:
```python
class MultiModelChecker:
    """多模型评分"""
    
    def __init__(self, models: List[BaseModel]):
        self.checkers = [
            Checker(gpt4_model),
            Checker(claude_model),
            Checker(gemini_model)
        ]
    
    async def check(self, source, draft_a, draft_b, segment_id):
        # 并发调用3个模型评分
        reports = await asyncio.gather(*[
            checker.check(source, draft_a, draft_b, segment_id)
            for checker in self.checkers
        ])
        
        # 取平均分
        avg_score = self._average_scores(reports)
        
        # 计算标准差（评估一致性）
        std_dev = self._calculate_std(reports)
        
        return avg_score, std_dev
```

**使用的模型**:
- GPT-4 (OpenAI via OpenRouter)
- Claude-3.5 (Anthropic via OpenRouter)
- Gemini-2.5-Pro (Google via OpenRouter)

**预期效果**:
- 评分更可靠
- 减少单一模型偏见
- 标准差<0.1表示评分一致性好

**实施时间**: 2-3小时

---

### 阶段2: 添加客观指标（优先级1）

**目标**: 补充模型主观评分，增加可量化指标

**实现方案**:
```python
class ObjectiveMetrics:
    """客观评估指标"""
    
    def length_ratio_score(self, source, target):
        """长度比例检查（异常检测）"""
        ratio = len(target) / len(source)
        if 0.5 <= ratio <= 2.0:
            return 1.0
        else:
            return 0.5  # 异常长度降低评分
    
    def terminology_match_score(self, source, target, glossary):
        """术语匹配度"""
        if not glossary:
            return 1.0
        matched = sum(1 for term, trans in glossary.items() 
                     if term in source and trans in target)
        return matched / len(glossary)
    
    def untranslated_detection(self, target, target_lang):
        """未翻译内容检测"""
        if target_lang == "zh":
            import re
            # 检查英文单词残留（排除专有名词）
            english_words = re.findall(r'\b[a-zA-Z]{3,}\b', target)
            # 过滤常见专有名词
            filtered = [w for w in english_words 
                       if w.lower() not in ['ai', 'ml', 'api', 'gpt']]
            if len(filtered) > 2:
                return 0.8  # 降低评分
        return 1.0
    
    def punctuation_check(self, source, target):
        """标点符号合理性"""
        # 检查中文标点使用
        # ...
        return 1.0
```

**混合评分公式**:
```python
final_score = (
    model_score * 0.7 +           # 模型评分权重70%
    length_score * 0.1 +          # 长度比权重10%
    terminology_score * 0.1 +     # 术语匹配权重10%
    untranslated_score * 0.1      # 未翻译检测权重10%
)
```

**预期效果**:
- 评分更全面
- 识别明显错误
- 评分更客观

**实施时间**: 1-2小时

---

### 阶段3: 动态评分提升（优先级2）

**目标**: 实际评估风格化效果，而非固定+0.05

**当前问题**:
```python
# 当前：固定提升
final_score = original_score + 0.05
```

**改进方案**:
```python
# 方案1: 再次评估风格化后的文本
before_score = checker_report.quality_scores[segment_id]
after_text = stylist_result.styled_text

# 使用Checker再次评估风格化后的文本
after_report = await self.checker.check_single(after_text)
after_score = after_report.quality_scores[segment_id]

# 实际提升
improvement = after_score - before_score
final_score = after_score

# 方案2: 基于修改记录计算提升
if stylist_result.terminology_changes:
    terminology_improvement = len(stylist_result.terminology_changes) * 0.01
if stylist_result.style_changes:
    style_improvement = len(stylist_result.style_changes) * 0.01

final_score = original_score + terminology_improvement + style_improvement
```

**预期效果**:
- 评分更准确
- 反映实际改进
- 提升值有依据

**实施时间**: 1小时

---

### 阶段4: 标准测试集与校准（优先级3）

**目标**: 建立评分基准，校准评分系统

**实施步骤**:

1. **下载标准测试集**
   - FLORES-200 (1000条)
   - WMT22 (500条)

2. **人工评分**
   - 抽样50-100条
   - 3人独立评分
   - 计算一致性

3. **评分校准**
   - 对比模型评分与人工评分
   - 计算偏差
   - 调整评分公式

```python
def calibrate_scores(model_scores, human_scores):
    """校准评分"""
    # 线性回归拟合
    from sklearn.linear_model import LinearRegression
    
    model = LinearRegression()
    model.fit(model_scores, human_scores)
    
    # 校准公式
    calibrated = model.predict(new_model_scores)
    return calibrated
```

**预期效果**:
- 评分与人工评分一致性高（相关性>0.8）
- 评分更可信

**实施时间**: 1-2周

---

### 阶段5: 细粒度错误分类（优先级3）

**目标**: 实现完整的MQM评分系统

**扩展评分维度**:

当前：
- Adequacy（充分性）
- Fluency（流畅性）
- Terminology（术语）

扩展：
- Grammar（语法错误）
- Spelling（拼写错误）
- Punctuation（标点错误）
- Style（风格不当）
- Consistency（一致性）

**错误权重系统**:
```python
error_weights = {
    "mistranslation": -5,      # 误译（严重）
    "omission": -3,            # 遗漏（中等）
    "grammar": -2,             # 语法错误
    "terminology": -3,         # 术语错误
    "style": -1,               # 风格不当（轻微）
}

# MQM分数 = 100 - 总扣分
mqm_score = max(0, 100 - total_penalty) / 100
```

**实施时间**: 2-3周

---

## 评估计划

### 每个阶段完成后的评估

1. **实施改进**
2. **运行评估**: `python evaluate_baseline.py`
3. **生成报告**
4. **对比V1结果**
5. **分析改进效果**

### 评估指标

| 指标 | V1基线 | V2目标 |
|-----|--------|--------|
| 平均BLEU | 0.82 | >0.85 |
| 平均系统评分 | 0.988 | 0.80-0.90 |
| BLEU相关性 | 0.10 | >0.50 |
| 评分标准差 | 0.020 | >0.05 |
| 评分范围 | 0.94-1.00 | 0.70-0.95 |

---

## V2改进路线图

### 第1周
- [x] 创建V2分支
- [ ] 实施多模型评分投票
- [ ] 添加客观指标
- [ ] 第一次V2评估

### 第2周
- [ ] 动态评分提升
- [ ] 修复已知bug（占位符、Planner语言检测）
- [ ] 扩大测试集（100条）
- [ ] 第二次V2评估

### 第3周
- [ ] 下载标准测试集
- [ ] 人工评分样本
- [ ] 评分校准
- [ ] 第三次V2评估

### 第4周
- [ ] 细粒度错误分类（可选）
- [ ] 论文撰写
- [ ] 最终评估报告

---

## 成功标准

V2版本达到以下标准即为成功：

1. **评分客观性** ✅
   - 多模型评分（≥3个模型）
   - 包含客观指标
   - 评分一致性好（标准差<0.1）

2. **评分准确性** ✅
   - BLEU相关性>0.5
   - 与人工评分相关性>0.7
   - 评分范围合理（0.7-0.95）

3. **评分区分度** ✅
   - 标准差>0.05
   - 能区分不同质量的翻译

---

## 当前状态

- ✅ V1分支已创建并推送（保存基线版本）
- ✅ V2分支已创建并推送（当前开发分支）
- ✅ 当前位于V2分支
- ✅ 准备开始改进工作

---

**文档版本**: V2.0  
**状态**: 规划中  
**下一步**: 实施多模型评分投票

